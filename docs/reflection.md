# Team Reflections

### 1. Huzaifa Imran (The Head Node)
* **Work:** I focused on the `AddTask` logic and initializing the Head pointer correctly.
* **Learned:** I learned how `new Node()` allocates memory dynamically in Heap.
* **Improvement:** If I had more time, I would implement file handling to save tasks permanently.

### 2. Abdul Rafay (The Pointer)
* **Work:** My main focus was linking nodes (`next` pointer) and managing the flow.
* **Learned:** I understood how losing a pointer link can cause a Memory Leak.
* **Improvement:** I would add a "Priority" feature to sort tasks by importance.

### 3. Hamza Aslam (The Data Keeper)
* **Work:** I worked on the `ViewTasks` display and making the output look clean.
* **Learned:** I learned about traversing a loop until `NULL` is reached.
* **Improvement:** I would add a simple GUI or colors to the console output.

### 4. Azan Zaman (The Null Terminator)
* **Work:** I handled the `DeleteTask` logic and boundary checks (Edge cases).
* **Learned:** I learned why checking `if(head == NULL)` is critical to prevent crashes.
* **Improvement:** I would add a "Delete All" button to clear the list instantly.
